import math

# Calculate the square root
sqrt_val = math.sqrt(25)
print(f"Square root of 25 is: {sqrt_val}")

# Calculate power
pow_val = math.pow(2, 3)
print(f"2 to the power of 3 is: {pow_val}")

# Value of Pi
pi_val = math.pi
print(f"Value of Pi is: {pi_val}")
